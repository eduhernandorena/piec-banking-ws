server:
  port: 8080

client:
  default-uri: https://cobranca.homologa.bb.com.br:7101/registrarBoleto
  ssl:
    trust-store: classpath:ca/jssecacerts

spring:
  application:
    name: piec-banking-ws-service
  cloud:
    config:
      enabled: false
